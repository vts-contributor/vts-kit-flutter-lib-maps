enum CoreMapType {
  google,
  @Deprecated('Viettel runtime UI implementation has been removed; this value currently falls back to Google. However, Viettel Map is still supported as a data provider.')
  viettel
}
