# 1.3
- Cluster: Fix duplicate markers with same id

# 1.2
- Add Route API caching
- Add more attributes for RouteConfig:
  - zIndex: static zIndex
  - selectOnTap: disable or enable select on tap (default is true)

# 1.1
- Fix clusters don't expand on max zoom for Google map

# 1.0 
- First version